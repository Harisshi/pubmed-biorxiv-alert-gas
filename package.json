{
  "name": "new-article-alert-with-gas",
  "version": "4.0.0",
  "description": "PubMed と bioRxiv の新着論文を検索し、LINE / Slack / メールへ通知する Google Apps Script",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build-bundle.js",
    "build:check": "node scripts/build-bundle.js --check",
    "lint": "node scripts/check-syntax.js",
    "test": "node --test test/*.test.js"
  }
}
