name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 構文チェック
        run: npm run lint

      - name: コピペ用ファイルが src と一致しているか確認
        run: npm run build:check

      - name: ユニットテスト
        run: npm test
