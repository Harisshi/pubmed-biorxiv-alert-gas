# 変更履歴

このプロジェクトの主な変更点を記録します。

## v4.0.0

公開リポジトリとして整備するための全面的な見直しです。

### 破壊的変更

- 単一ファイル `コード.js` を `src/` 配下の役割別ファイルに分割しました。
- API キーやアクセストークンなどの秘密情報は、コード内の `SETTINGS` ではなく
  **スクリプトプロパティ**から読み込むようになりました。
- ユーザー設定は `src/UserConfig.example.js` をコピーして作る
  `src/UserConfig.js` に移動しました。
- スプレッドシートに `NotifyStatus` 列と `MatchedKeywords` 列を追加しました。
  既存のシートには、実行時に自動で列が追加されます。
- 定期実行の入口を `runDailySearch()` に統一しました。
  この関数は「前日 1 日分」ではなく、設定した `searchPeriod` の期間を検索します。
  期間を指定して取り込みたい場合は `runSearchForPeriod(開始日, 終了日)` を使います。

### 不具合の修正

- 複数のキーワードにヒットした論文が、同じ実行の中で重複して記録されていた問題を修正しました。
- 英語要約（`SummarizedAbstract`）が通知に反映されていなかった問題を修正しました。
- Gemini API の呼び出し回数が実際の 2 倍で記録されていた問題を修正しました。
- bioRxiv の取得が先頭 100 件で打ち切られていた問題を修正し、期間内の全件を取得するようにしました。
- 同じ bioRxiv の全件リストをキーワードの数だけ取得していた無駄を解消しました。
- 通知件数の表示が 5 年インパクトファクターを考慮しておらず、実際の通知内容と食い違っていた問題を修正しました。
- インパクトファクターが基準に満たない論文にも通知日時が記録され、
  基準を変更しても再通知できなくなっていた問題を修正しました（`NotifyStatus` 列で区別します）。
- 定期実行の入口でトリガー回数が加算されず、1 日あたりの上限が機能していなかった問題を修正しました。
- `UrlFetchApp.fetch` に GAS が解釈しない `timeout` オプションを渡していた箇所を削除しました。
- メッセージ分割時に区切り線が失われ、1 件で上限を超える論文を分割できなかった問題を修正しました。
- LINE / Slack への送信結果を確認しないまま「通知済み」として記録していた問題を修正しました。

### 改善

- インパクトファクターの照合を、実行ごとに CSV を 1 度だけ読み込む索引方式に変更しました。
  以前は論文 1 件ごとに CSV 全体を読み直して全行と比較していたため、
  論文数が増えると実行時間の上限に達していました。
- PubMed の取得を esearch + efetch のまとめ取得に変更し、リクエスト数を大幅に削減しました。
- PubMed の日付の解釈を `edat`（PubMed への登録日）に変更しました。`pubmedDateType` で変更できます。
- medRxiv の検索に対応しました（`bioRxivServers`）。
- Slack 通知を実用可能な状態にしました（Webhook URL の検証、文字数上限に応じた分割、
  mrkdwn のエスケープ、送信結果の確認）。
- HTTP リクエストに指数バックオフによる再試行を追加しました。
- Gemini API キーを URL ではなくリクエストヘッダーで送るようにしました。
- 設定内容を確認する `showConfigurationStatus()`、通知内容を確認する `previewNotification()`、
  疎通確認用の `sendTestNotification()`、トリガーを作成する `createDailyTrigger()` を追加しました。
- 純粋関数に対するユニットテストと GitHub Actions による CI を追加しました。

## v3.0.3

- インパクトファクターの参照方法を変更し、5 年インパクトファクターに対応。
- bioRxiv の通知を修正。

## v3.0.0

- PubMed E-utilities と bioRxiv API から情報を取得する実装に刷新。
